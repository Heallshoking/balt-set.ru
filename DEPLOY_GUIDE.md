# 📋 Пошаговая инструкция деплоя на Timeweb App Platform

## ⚡ Быстрая версия (30 минут)

### ШАГ 1: Загрузить на GitHub (10 минут)

```bash
# Перейти в папку проекта
cd "/Users/user/Documents/Проекты/Балтсеть - Переезд в USA/ai-service-platform"

# Инициализация git
git init

# Добавить все файлы
git add .
git commit -m "Initial commit: AI Service Platform ready for Timeweb"

# Создать репозиторий на GitHub.com
# Перейдите на https://github.com/new
# Создайте репозиторий (например: ai-service-platform)
# НЕ добавляйте README, .gitignore (они уже есть)

# Подключить удалённый репозиторий (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/ai-service-platform.git
git branch -M main
git push -u origin main
```

---

### ШАГ 2: Создать приложение в Timeweb (15 минут)

#### 2.1. Открыть панель создания

Перейдите: https://timeweb.cloud/my/apps/create

#### 2.2. Выбрать тип приложения

- ✅ Нажмите вкладку **"Backend"**
- ✅ В фильтре выберите **"Python"**
- ✅ Выберите карточку **"FastAPI"** (Python 3.14)

#### 2.3. Подключить репозиторий

Нажмите **"Далее"**, затем:

1. Выберите **"GitHub"**
2. Авторизуйте Timeweb (если первый раз)
3. Выберите репозиторий **"ai-service-platform"**
4. Выберите ветку **"main"**
5. ✅ Включите **"Автодеплой при push"**

#### 2.4. Настроить конфигурацию

**Регион:** Москва

**Тариф:**
- Для начала: **90 ₽/мес** (1 ГБ RAM, 200k запросов)
- Параметры на экране:
  - CPU: 1 x 3.3 ГГц
  - RAM: 1 ГБ
  - NVMe: 1-2 ГБ

**Команда запуска:**
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

**Команда сборки:** (оставить пустым)

#### 2.5. Добавить переменные окружения

Нажмите **"+ Добавить переменную"**

**Минимально необходимые:**

1. **Первая переменная:**
   ```
   Название: DEBUG
   Значение: false
   ```
   → Нажмите **"Сохранить"**

2. **Вторая переменная:**
   ```
   Название: ENVIRONMENT
   Значение: production
   ```
   → Нажмите **"Сохранить"**

3. **Третья переменная:**
   ```
   Название: DATABASE_PATH
   Значение: /app/data/ai_service.db
   ```
   → Нажмите **"Сохранить"**

**Опционально** (если есть ключи API):

4. **OpenAI API Key:**
   ```
   Название: OPENAI_API_KEY
   Значение: ваш_ключ_openai
   ```

5. **Telegram Bot Token:**
   ```
   Название: TELEGRAM_BOT_TOKEN
   Значение: ваш_токен_бота
   ```

#### 2.6. Запустить деплой

1. Проверьте все настройки
2. Нажмите **"Заказать"** / **"Создать приложение"**
3. Дождитесь завершения деплоя (5-10 минут)

---

### ШАГ 3: Проверка работы (5 минут)

После успешного деплоя:

#### 3.1. Получить URL приложения

Timeweb покажет URL вашего приложения, например:
```
https://your-app-name.timeweb.cloud
```

#### 3.2. Проверить endpoints

**Главная страница (форма для клиентов):**
```
https://your-app-name.timeweb.cloud/
```

**Health check:**
```
https://your-app-name.timeweb.cloud/health
```

**API документация:**
```
https://your-app-name.timeweb.cloud/docs
```

**API info:**
```
https://your-app-name.timeweb.cloud/api
```

---

## ✅ ГОТОВО!

Ваше приложение работает! 🎉

---

## 🔄 Как обновлять приложение

После любых изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Timeweb автоматически развернёт новую версию за 2-5 минут! ⚡

---

## 🧪 Тестирование

### Создать тестового мастера

Откройте терминал:

```bash
curl -X POST https://your-app-name.timeweb.cloud/api/v1/masters/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Тестовый Мастер",
    "phone": "+79001111111",
    "specializations": ["electrical", "plumbing"],
    "city": "Москва",
    "preferred_channel": "telegram"
  }'
```

Ответ:
```json
{
  "success": true,
  "master_id": 1,
  "message": "Мастер Тестовый Мастер успешно зарегистрирован",
  "terminal_url": "/terminal/1"
}
```

### Активировать терминал мастера

```bash
curl -X POST https://your-app-name.timeweb.cloud/api/v1/masters/1/activate-terminal
```

### Отправить тестовую заявку

Откройте в браузере:
```
https://your-app-name.timeweb.cloud/
```

Заполните форму и отправьте!

---

## 🚨 Возможные проблемы

### Проблема 1: Деплой упал с ошибкой

**Решение:**
1. Проверьте логи в панели Timeweb
2. Убедитесь, что `requirements.txt` корректный
3. Проверьте, что команда запуска правильная

### Проблема 2: 502 Bad Gateway

**Решение:**
1. Подождите 2-3 минуты (приложение запускается)
2. Проверьте логи
3. Убедитесь, что порт `$PORT` используется в команде запуска

### Проблема 3: База данных не создаётся

**Решение:**
Убедитесь, что переменная `DATABASE_PATH` установлена:
```
DATABASE_PATH=/app/data/ai_service.db
```

---

## 📞 Поддержка Timeweb

Если что-то не получается:

1. **Документация:** https://timeweb.cloud/docs/apps
2. **Чат поддержки:** В личном кабинете Timeweb
3. **Telegram:** @twcsupport
4. **Email:** info@timeweb.cloud

---

## 🎯 Что дальше?

После успешного деплоя можно:

1. ✅ Добавить свой домен
2. ✅ Настроить SSL (бесплатно от Timeweb)
3. ✅ Подключить Telegram бот
4. ✅ Добавить OpenAI для умного AI
5. ✅ Настроить мониторинг
6. ✅ Масштабировать при росте нагрузки

---

**Удачного деплоя! 🚀**
