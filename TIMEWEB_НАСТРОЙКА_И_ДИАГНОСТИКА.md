# 🔧 TIMEWEB - НАСТРОЙКА И ДИАГНОСТИКА

**Дата:** 13 декабря 2025  
**Проект:** app.balt-set.ru  
**Проблема:** Деплои не применяются на production

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### ✅ Что работает:
```bash
✅ GitHub синхронизация: origin → https://github.com/Heallshoking/balt-set.ru.git
✅ Последний коммит: a0be4a2 (FIX: Отключена Google интеграция)
✅ /health endpoint: 200 OK
✅ /api endpoint: 200 OK (возвращает JSON)
✅ Главная /: 200 OK
```

### ❌ Что НЕ работает:
```bash
❌ /admin → 404 (должен возвращать HTML)
❌ /master → 404 (должен возвращать HTML)
❌ /ai-chat → 404 (должен возвращать HTML)
❌ /track → 404 (должен возвращать HTML)
```

### 🔍 Диагностика:
- **Локально**: Все маршруты работают ✅
- **На production**: Маршруты возвращают 404 ❌
- **Деплоев сделано**: 6 штук за последние 2 часа
- **Результат**: Ни один не применился!

---

## 🎯 КАК РАБОТАЕТ СВЯЗКА

### Схема деплоя:

```
Локальный код (main.py)
    ↓
git commit + push → GitHub (main ветка)
    ↓
GitHub Webhook → Timeweb App Platform
    ↓
Timeweb пересобирает и перезапускает приложение
    ↓
app.balt-set.ru обновляется (2-3 минуты)
```

### Текущая проблема:

**GitHub → Timeweb** связь НЕ РАБОТАЕТ!

Деплои попадают на GitHub, но Timeweb их не применяет.

---

## 🔧 ПРОВЕРКА НАСТРОЕК TIMEWEB

### ШАГ 1: Зайти в панель Timeweb

1. Откройте: https://timeweb.cloud/my/apps
2. Найдите приложение `balt-set.ru`
3. Кликните на него

### ШАГ 2: Проверить настройки деплоя

В настройках приложения проверьте:

#### 2.1 **GitHub подключение**

```
✅ Должно быть:
   Repository: Heallshoking/balt-set.ru
   Branch: main
   Auto-deploy: Включен
```

❌ **Если не подключено:**
1. Нажмите "Connect to GitHub"
2. Выберите репозиторий `Heallshoking/balt-set.ru`
3. Выберите ветку `main`
4. Включите "Auto-deploy on push"

#### 2.2 **Build Command**

```
✅ Должно быть пусто или:
   pip install -r requirements.txt
```

#### 2.3 **Start Command**

```
✅ Должно быть:
   uvicorn main:app --host 0.0.0.0 --port $PORT
```

или через Procfile:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

#### 2.4 **Environment Variables**

```
✅ Обязательные:
   ENVIRONMENT=production
   DATABASE_PATH=/app/data/ai_service.db

⚠️ Опциональные:
   DEBUG=false
   PLATFORM_COMMISSION_RATE=0.25
```

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМЫ

### Проверка 1: Логи деплоя

В панели Timeweb откройте вкладку **"Deployments"** или **"Логи"**:

**Ищите:**
- ✅ Последний деплой с commit hash `a0be4a2`
- ✅ Статус: Success / Running
- ❌ Ошибки в логах

**Если деплоя нет:**
→ GitHub webhook не работает!

### Проверка 2: Build логи

Откройте **"Build Logs"**:

**Ищите:**
```bash
✅ Installing dependencies from requirements.txt
✅ Starting application: uvicorn main:app
✅ Application started successfully
```

**Если есть ошибки:**
```bash
❌ ModuleNotFoundError
❌ SyntaxError
❌ Permission denied
```
→ Проблема в коде или зависимостях!

### Проверка 3: Runtime логи

Откройте **"Runtime Logs"** (логи работающего приложения):

**Ищите:**
```bash
✅ INFO: Started server process
✅ INFO: Application startup complete
✅ INFO: Uvicorn running on http://0.0.0.0:$PORT
```

**Если есть:**
```bash
❌ ModuleNotFoundError: No module named 'google_sync'
❌ ImportError: ...
❌ Exception in startup
```
→ Проблема при запуске!

### Проверка 4: GitHub Webhook

В GitHub репозитории:

1. Зайдите: https://github.com/Heallshoking/balt-set.ru/settings/hooks
2. Проверьте есть ли webhook от Timeweb
3. Кликните на webhook → "Recent Deliveries"
4. Проверьте статус последних деплоев

**Должно быть:**
```
✅ Response code: 200
✅ Payload delivered successfully
```

**Если:**
```
❌ Response code: 404
❌ Timeout
❌ Connection refused
```
→ Webhook не работает, нужно переподключить GitHub!

---

## ⚡ РЕШЕНИЯ

### Решение 1: Ручной перезапуск

**Самое простое:**

1. В панели Timeweb найдите приложение
2. Нажмите кнопку **"Restart"** или **"Rebuild"**
3. Подождите 2-3 минуты
4. Проверьте: `curl https://app.balt-set.ru/admin`

### Решение 2: Переподключить GitHub

Если auto-deploy не работает:

1. В настройках приложения **отключите** GitHub
2. Подождите 10 секунд
3. **Подключите** GitHub заново
4. Выберите репозиторий и ветку `main`
5. Включите "Auto-deploy on push"
6. Сделайте пустой commit для теста:

```bash
git commit --allow-empty -m "Test deploy"
git push origin main
```

### Решение 3: Проверить Procfile

Убедитесь что файл `Procfile` правильный:

```bash
cat Procfile
# Должно вывести:
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

Если файла нет - создайте:

```bash
echo "web: uvicorn main:app --host 0.0.0.0 --port \$PORT" > Procfile
git add Procfile
git commit -m "Add Procfile"
git push origin main
```

### Решение 4: Форсированный деплой через CLI

Если ничего не помогает, используйте Timeweb CLI:

#### 4.1 Установка CLI

```bash
# Если есть Node.js
npm install -g @timeweb-cloud/twc-cli

# Или через pip
pip install timeweb-cloud-cli
```

#### 4.2 Авторизация

```bash
twc login
# Введите API токен из https://timeweb.cloud/my/api-keys
```

#### 4.3 Деплой

```bash
# Список приложений
twc apps list

# Деплой конкретного приложения
twc apps deploy --app balt-set.ru --branch main
```

---

## 📋 ЧЕКЛИСТ НАСТРОЙКИ

### Перед запуском проверьте:

```
✅ GitHub репозиторий подключен к Timeweb
✅ Ветка main выбрана для деплоя
✅ Auto-deploy включен
✅ Webhook от Timeweb есть в GitHub
✅ Procfile или Start Command настроен
✅ Environment переменные установлены
✅ requirements.txt актуален
✅ Нет синтаксических ошибок в коде
```

### После деплоя проверьте:

```bash
# Автоматический тест
./check_production.sh

# Или вручную:
curl https://app.balt-set.ru/health
curl https://app.balt-set.ru/api
curl https://app.balt-set.ru/admin | head -20
```

---

## 🚨 ТИПИЧНЫЕ ПРОБЛЕМЫ

### Проблема 1: Google OAuth блокирует запуск

**Симптомы:**
- Логи показывают: "Please visit this URL to authorize"
- Приложение не стартует
- Все endpoints возвращают 404

**Решение:**
```python
# В main.py отключите Google:
GOOGLE_SYNC_AVAILABLE = False
```

✅ **УЖЕ ИСПРАВЛЕНО** в коммите `a0be4a2`

### Проблема 2: Static файлы не находятся

**Симптомы:**
- `/admin`, `/master` возвращают 404
- Локально работает

**Решение:**
```python
# Использовать fallback HTML вместо FileResponse
# (уже реализовано в коммите c472890)
```

### Проблема 3: Порт не динамический

**Симптомы:**
- Приложение запускается, но Timeweb не видит
- В логах: "Address already in use"

**Решение:**
```python
# Использовать переменную окружения $PORT
import os
port = int(os.getenv("PORT", 8000))
```

В Start Command:
```bash
uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

### Timeweb
- 🛠️ **Панель приложений**: https://timeweb.cloud/my/apps
- 📚 **Документация**: https://timeweb.cloud/help/app-platform
- 🔑 **API Keys**: https://timeweb.cloud/my/api-keys
- 📊 **Логи**: В приложении → вкладка "Logs"

### GitHub
- 💻 **Репозиторий**: https://github.com/Heallshoking/balt-set.ru
- 🔗 **Webhooks**: https://github.com/Heallshoking/balt-set.ru/settings/hooks
- 📝 **Commits**: https://github.com/Heallshoking/balt-set.ru/commits/main

### Локальные файлы
- 📂 **Workspace**: `/Users/user/Documents/Projects/Github/balt-set.ru`
- 🚀 **Quick Deploy**: `./quick_deploy.sh "message"`
- 🧪 **Check Production**: `./check_production.sh`

---

## 💡 ИТОГОВАЯ РЕКОМЕНДАЦИЯ

**Сейчас нужно сделать:**

1. **Зайти в Timeweb панель**: https://timeweb.cloud/my/apps
2. **Найти приложение** `balt-set.ru`
3. **Проверить:**
   - GitHub подключен?
   - Auto-deploy включен?
   - Есть ли последний деплой в списке?
4. **Если деплоев нет** → Переподключить GitHub
5. **Если деплои есть, но не работают** → Нажать "Restart"
6. **Проверить логи** на наличие ошибок

После этого всё должно заработать! ✨

---

**Последнее обновление:** 13 декабря 2025, 10:03  
**Статус:** Google интеграция отключена, код локально работает идеально
