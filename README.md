# 🚀 AI Service Platform

**Автоматизированная платформа для связи мастеров и клиентов**

Оптимизирована для быстрого деплоя на Timeweb App Platform.

---

## 🎯 БЫСТРЫЙ СТАРТё

### 👉 **[НАЧНИТЕ ЗДЕСЬ - START_HERE.md](START_HERE.md)** 👈

**Выберите свой уровень:**
- 🟢 **Новичок?** → [ШПАРГАЛКА.md](ШПАРГАЛКА.md) - Деплой за 20 минут!
- 🟡 **Хотите детали?** → [ПРОСТАЯ_ИНСТРУКЦИЯ.md](ПРОСТАЯ_ИНСТРУКЦИЯ.md)
- 🔵 **Нужен чеклист?** → [ЧЕКЛИСТ.md](ЧЕКЛИСТ.md)

---

---

## ✨ Возможности

- ✅ **Регистрация мастеров** - простая форма регистрации
- ✅ **AI-обработка заявок** - автоматический анализ проблем
- ✅ **Автоподбор мастера** - по специализации и рейтингу
- ✅ **Расчёт цен** - автоматическое ценообразование
- ✅ **Терминал мастера** - мобильный интерфейс для работы
- ✅ **Обработка платежей** - с автоматическим расчётом комиссий
- ✅ **SQLite база данных** - работает без настройки

---

## 🚀 Быстрый старт (локально)

### 1. Клонировать репозиторий

```bash
git clone <your-repo-url>
cd ai-service-platform
```

### 2. Создать виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Запустить сервер

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

- **Форма для клиентов**: http://localhost:8000
- **API документация**: http://localhost:8000/docs
- **API info**: http://localhost:8000/api

---

## 🌐 Деплой на Timeweb App Platform

### Вариант 1: Через GitHub (рекомендуется)

#### Шаг 1: Создать GitHub репозиторий

```bash
# Инициализация git (если ещё не сделано)
git init

# Добавить все файлы
git add .
git commit -m "Initial commit: AI Service Platform"

# Подключить удалённый репозиторий
git remote add origin https://github.com/YOUR_USERNAME/ai-service-platform.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Создать приложение в Timeweb

1. Перейдите на https://timeweb.cloud/my/apps/create
2. Выберите **Backend** → **FastAPI** (Python 3.14)
3. Подключите GitHub репозиторий
4. Выберите ветку `main`
5. Включите **автодеплой**

#### Шаг 3: Настройка конфигурации

**Регион:** Москва  
**Тариф:** 90 ₽/мес (1 ГБ RAM, 200k запросов/месяц)

**Команда запуска:**
```bash
uvicorn main:app --host 0.0.0.0 --port $PORT
```

#### Шаг 4: Добавить переменные окружения

В разделе **"Переменные"** добавьте:

```
DEBUG=false
ENVIRONMENT=production
DATABASE_PATH=/app/data/ai_service.db
```

Опционально (если есть):
```
OPENAI_API_KEY=your_key
TELEGRAM_BOT_TOKEN=your_token
```

#### Шаг 5: Запустить деплой

Нажмите **"Создать приложение"** и дождитесь завершения (5-10 минут).

### Вариант 2: Через Docker (если нужен полный контроль)

1. В интерфейсе Timeweb выберите вкладку **Docker**
2. Подключите репозиторий
3. Укажите `Dockerfile` (создайте если нужно)
4. Запустите деплой

---

## 📊 API Endpoints

### Клиенты

```bash
# Отправить заявку
POST /api/v1/ai/web-form
{
  "name": "Иван Иванов",
  "phone": "+79001234567",
  "category": "electrical",
  "problem_description": "Не работает розетка",
  "address": "ул. Тестовая 1"
}
```

### Мастера

```bash
# Регистрация
POST /api/v1/masters/register

# Активировать терминал
POST /api/v1/masters/{id}/activate-terminal

# Список доступных мастеров
GET /api/v1/masters/available/{category}
```

### Терминал мастера

```bash
# Получить заказы
GET /api/v1/terminal/jobs/{master_id}

# Активный заказ
GET /api/v1/terminal/jobs/{master_id}/active

# Обновить статус
PATCH /api/v1/terminal/jobs/{master_id}/status/{job_id}

# Принять оплату
POST /api/v1/terminal/payment/process

# Заработок
GET /api/v1/terminal/earnings/{master_id}
```

### Статистика

```bash
# Общая статистика
GET /api/v1/stats
```

---

## 💰 Финансовая модель

**Расчёт комиссий (пример для 3000₽):**

| Участник | Сумма | % |
|----------|-------|---|
| Клиент платит | 3000₽ | 100% |
| Комиссия платёжного шлюза | -60₽ | -2% |
| **Остаток** | **2940₽** | **98%** |
| Комиссия платформы | -735₽ | -25% от остатка |
| **Мастер получает** | **2205₽** | **73.5%** |

---

## 🔧 Настройки

Все настройки через переменные окружения (см. `.env.example`).

**Основные:**
- `DEBUG` - режим отладки (false для продакшена)
- `ENVIRONMENT` - окружение (production/development)
- `DATABASE_PATH` - путь к SQLite базе
- `PLATFORM_COMMISSION_RATE` - комиссия платформы (0.25 = 25%)

---

## 📱 Интерфейсы

- **/** - Форма для клиентов (веб-интерфейс)
- **/docs** - Swagger документация API
- **/api** - Информация об API

---

## 🗂️ Структура базы данных

**SQLite** (автоматически создаётся при первом запуске):

- `masters` - мастера
- `jobs` - заказы
- `transactions` - платежи

---

## 🧪 Тестирование

### Создать тестового мастера

```bash
curl -X POST https://your-app.timeweb.cloud/api/v1/masters/register \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Иван Электрик",
    "phone": "+79001234567",
    "specializations": ["electrical"],
    "city": "Москва",
    "preferred_channel": "telegram"
  }'
```

### Отправить заявку

```bash
curl -X POST https://your-app.timeweb.cloud/api/v1/ai/web-form \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Клиент Тестовый",
    "phone": "+79002222222",
    "category": "electrical",
    "problem_description": "Не работает розетка в гостиной",
    "address": "ул. Тестовая 1, кв. 10"
  }'
```

---

## 🚀 Производительность

**Текущая конфигурация подходит для:**
- До 1000 пользователей/месяц
- До 200k API запросов/месяц
- Работает на тарифе 90 ₽/мес Timeweb App Platform

**Для масштабирования:**
- Увеличьте тариф до 801 ₽/мес (2 млн запросов)
- Переключитесь на PostgreSQL (для высоких нагрузок)
- Добавьте Redis для кэширования

---

## 📈 Расширение функционала

**Легко добавить:**
- ✅ OpenAI интеграция (раскомментируйте в requirements.txt)
- ✅ Telegram бот (добавьте TOKEN в переменные)
- ✅ Анализ фото (добавьте Pillow)
- ✅ Email уведомления
- ✅ Push-уведомления

---

## 🤝 Поддержка

**Проблемы с деплоем?**
1. Проверьте логи в Timeweb панели
2. Убедитесь, что все переменные окружения установлены
3. Проверьте `/health` endpoint

---

## 📄 Лицензия

Проект создан в образовательных целях.

---

**Готово к деплою! 🚀**

Создано для Timeweb App Platform
